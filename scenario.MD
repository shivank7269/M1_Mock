You are building a Student Management API for an internship program.
Tasks:
1.	Create an API with the following endpoints:
    ○	GET /students → Return all students
    ○	POST /students → Add a new student
    ○	GET /students/{id} → Fetch a specific student
    ○	PUT /students/{id} → Update student details
    ○	DELETE /students/{id} → Delete a student
2.	Requirements:
○	Use FastAPI or Flask
○	Validate request body (name, age, course)
○	Return proper HTTP status codes
○	Use JSON responses
👉 Bonus:
●	Add query parameter filtering (?course=AI)
